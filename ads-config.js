"use strict";

// Cấu hình độc lập với core Practice/Exam.
// Đổi enabled thành false để tắt hoàn toàn lớp quảng cáo.
window.ADS_CONFIG = Object.freeze({
  enabled: true,
  provider: "placeholder",
  position: "bottom",
  maxHeight: 50,
  placeholderHeight: 50,
  placeholderText: "VỊ TRÍ QUẢNG CÁO · BẢN THỬ"
});
